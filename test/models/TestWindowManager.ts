/// <reference path="../../app/models/WindowManager.ts"/>
/// <reference path="../../typings/main/ambient/jasmine/jasmine.d.ts"/>

//import wm = require("../../app/models/WindowManager");
/*
describe("WindowManager classは, ", () => {
    beforeEach(() => {
        this.windowManager = wm.WindowManager.getManager();
    });

    it("常に同じsingleton", () => {
        let result = this.windowManager;
        expect(result).toBe(wm.WindowManager.getManager());
    });

    it("作って消す", () => {
      this.windowManager.create("name1");
      this.windowManager.closeWindowName("name1");
      let result = this.getWindowNames();
      expect(result).toBe([]);
    });

});*/
